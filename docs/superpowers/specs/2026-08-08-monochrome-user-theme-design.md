# Monochrome User Theme Design

Create a personal Plif theme with a charcoal visual base, white primary text, and grayscale-only hierarchy. Semantic states use brightness and weight rather than hue. The theme lives in `~/.plif`, is selected in the user's existing config, and remains local.
