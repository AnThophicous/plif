# Histórico de atualizações — pli'ef argus

Este arquivo é cumulativo. A próxima IA deve preservar as entradas anteriores e acrescentar sua própria entrada. Correções devem ser adicionadas como novas linhas, sem apagar o histórico original.

Nota de linhagem: esta skill nasceu como `pli'ef float` (cybersecurity defensiva). Na versão 3.0.0 ela incorporou o pacote externo `plief-argus` (SecurityIR, caminhos de ataque, SecDiff, prova de correção, postura e portão de release) e adotou o nome `argus`. As duas primeiras entradas abaixo são o histórico da `float`, preservado na íntegra por continuidade — a pasta `plief-float/` permanece no repositório marcada como descontinuada.

Nota: a entrada de 2026-08-29 foi registrada no formato antigo (hora local sem fuso, sem referência de commit) e foi transcrita sem alteração de conteúdo.

| Data e hora (ISO 8601) | IA utilizada | Referência | Atualizações |
|---|---|---|---|
| 2026-08-29 16:58:39 (fuso não informado) | Codex | — | Criada a `pli'ef float` para cybersecurity defensiva e auditoria autorizada, com threat modeling, revisão de controles, validação segura, hardening, priorização de risco e limites explícitos contra uso ofensivo indevido. |
| 2026-09-02T20:45:00Z | Claude (Cursor Cloud Agent) | `cfa2c10` | Versão 2.0.0. Pasta renomeada de `pli'ef float/` para `plief-float/` para coincidir com `name`. `SKILL.md` reescrito: autossuficiente (sem dependência de arquivos da raiz), `description` com gatilhos de uso (scanners, CVE, segredo vazado, hardening, auth, compliance), `license` e `metadata` com prefixo `plief-`, limite de autorização preservado e reforçado, quando usar/não usar, seção de recursos, preparação obrigatória com mascaramento de segredos, fluxo em 7 etapas (superfície, ameaças, controles em três estados, validação segura, classificação, correção de causa raiz, comunicação), critérios de pronto, estrutura fixa de entrega. Criados `references/modelo-de-achado.md` (template de achado e de relatório), `references/matriz-de-severidade.md` (impacto × facilidade × pré-requisitos, ajustes, SLA, mapeamento CVSS, erros comuns), `references/checklist-controles.md` (13 áreas alinhadas a ASVS/Top 10), `references/checklist-hardening.md` (headers, cookies, CORS, TLS, Docker/K8s, CI/CD, dependências, segredos, banco, framework), `references/resposta-a-segredo-vazado.md` (procedimento e casos específicos), `references/exemplos.md` (3 casos). Motivo: elevar a skill a padrão profissional e resolver os itens C1, C2, A1, M1, M2, M3 do `relatorio.md`. |
| 2026-09-02T22:52:15Z | Claude Opus 5 (Claude Code) | pendente | Versão 3.0.0, criada a pasta `plief-argus/` pela fusão da `plief-float` com o pacote externo `plief-argus`, adotando o nome `argus`. Incorporados os quatro motores executáveis em `scripts/` (`argus_ir_lint.py`, `argus_attackpath.py`, `argus_sec_diff.py`, `argus_findings.py`), os schemas e os casos de avaliação em `assets/`, e traduzidos para português os documentos do núcleo do Argus, agora em `references/`: `security-ir.md`, `caminhos-de-ataque.md`, `ciclo-de-achados.md`, `postura.md`, `portao-de-release.md`, `identidade.md`, `seguranca-de-ia.md` e `matriz-por-tipo-de-projeto.md`. Preservados sem alteração os seis arquivos de `references/` da `float`. Criado `references/ponte-checklist-ir.md`, que é a integração de fato: converte cada item de checklist em nó `Control` do SecurityIR mantendo os três estados (presente, ausente, não avaliado) e mostra de onde vem cada um dos três eixos do achado. `SKILL.md` reescrito com fluxo de 10 etapas (grafo, ameaças, controles convertidos em nós, caminhos de ataque, validação segura, classificação, causa raiz, prova de correção, SecDiff, postura e portão) e seis modos de operação; limite de autorização da `float` preservado e reforçado com o contrato de campos obrigatórios do Argus. Estrutura adaptada à convenção do repositório: `core/`, `modules/`, `tools/`, `evals/` e `manifest.json` do pacote original foram redistribuídos em `references/`, `assets/` e `scripts/`, com os caminhos internos dos scripts atualizados. Vocabulário de máquina (tipos de nó e aresta, estados do ciclo de vida, rótulos de classificação de caminho) mantido em inglês de propósito: os scripts o verificam como literal, e traduzi-lo quebraria os motores. Verificação executada: `--selftest` dos quatro motores passou, e `scripts/validate.py` passou sem erros. Motivo: unir a cobertura de controles e a disciplina de comunicação da `float` ao modelo percorrível e à verificação mecânica do Argus, eliminando a lacuna entre "o controle existe" e "o controle está no caminho". Três defeitos foram encontrados e corrigidos nos motores durante o teste de ponta a ponta: `argus_attackpath.py` e `argus_sec_diff.py` gravavam no destino padrão sob `.plif/artifacts/` sem criar o diretório, falhando com `FileNotFoundError` depois de a análise já ter rodado; e `argus_findings.py` só fazia `json.load`, de modo que não lia o `findings.jsonl` que o próprio fluxo produz, e, ao receber um JSONL de uma linha (que desserializa como dict), caía no ramo do envelope, verificava zero registros e imprimia `OK: lifecycle valid` — uma garantia falsa, agora substituída por erro explícito e pela contagem de achados verificados. Duas afirmações da documentação também foram corrigidas contra o comportamento real medido: a classificação de caminho usa `provenance.ref` e `verified`, e não `confidence`; e controles ligados ao nó final da cadeia ficam fora da classificação, então a aresta `protected_by` precisa apontar para o salto. Limitação registrada: o SecDiff compara arestas de proteção pelo par (alvo, controle), então mover um controle aparece como `weakened_control` bloqueante mesmo quando a mudança melhora a proteção. |
